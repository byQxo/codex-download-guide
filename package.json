{
  "name": "yudidc-codex-download",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -k \"vite --host 0.0.0.0\" \"node --watch server/index.mjs\"",
    "build": "vite build",
    "start": "node server/index.mjs",
    "mirror": "node scripts/mirror-site.mjs",
    "verify": "node scripts/verify-mirror.mjs",
    "test:e2e": "playwright test",
    "test": "npm run verify"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "concurrently": "^9.1.2",
    "vite": "^7.1.5"
  }
}

